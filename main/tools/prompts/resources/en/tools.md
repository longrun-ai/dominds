# resources Tools Reference

### list_resources

List available resources and resource templates. Optional filters: `serverId`, `query`, `kind`.

### fetch_resource

Fetch a resource by `resourceId`. For `resource_template` entries, pass `arguments` with string
values for the template variables shown by `list_resources`.
